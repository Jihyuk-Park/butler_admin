import { Box } from '@mui/material';
import { styled } from '@mui/system';

/** width, padding을 포함한 fixedBox  */
export default styled(Box)({
  width: 'inherit',
  position: 'fixed',
  paddingRight: '5px',
});
